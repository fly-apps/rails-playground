<%= render_layout "application" do %>
  <header class="flex flex-row items-center justify-center gap-x-8 py-12">
    <%= image_tag "brandmark.svg", class: "max-w-[6rem]" %>
    <h1 class="font-bold text-4xl"><%= current_page.data.fetch("title") %></h1>
  </header>
  <article class="prose max-w-none">
    <%= yield %>
  </article>
<% end %>
